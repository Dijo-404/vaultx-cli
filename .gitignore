/target
node_modules/
*.log
.DS_Store

# downloaded release artifacts (fetched from GitHub Releases to embed checksums)
/dist/

# accidental artifacts from running npm inside the package dir
npm/package-lock.json
