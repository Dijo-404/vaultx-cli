//! Remote control-plane service (workspaces, projects, devices,
//! objects, refs, agents, audit events).

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {
        let ok = true;
        assert!(ok);
    }
}
