//! Structured event schema, append-only storage interface, remote
//! ingestion interface, export, redaction guarantees.

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {
        let ok = true;
        assert!(ok);
    }
}
