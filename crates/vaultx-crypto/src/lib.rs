//! Authenticated encryption, envelope key wrapping, signatures, keyed
//! fingerprints, secret-safe wrapper types.

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {
        let ok = true;
        assert!(ok);
    }
}
