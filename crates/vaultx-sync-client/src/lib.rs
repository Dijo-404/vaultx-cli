//! Workspace auth, object exchange, ref synchronization, signature
//! verification, conflict detection.

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {
        let ok = true;
        assert!(ok);
    }
}
