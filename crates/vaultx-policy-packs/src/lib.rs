//! Declarative pack parser, schema validation,
//! pack-to-generic-policy compilation, credential injection templates.

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {
        let ok = true;
        assert!(ok);
    }
}
